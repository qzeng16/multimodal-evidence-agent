# Required for model-backed verification.
OPENAI_API_KEY=your-openai-api-key

# Optional locally, but recommended for every public deployment.
# Clients send this value through the X-API-Key request header.
SERVICE_API_KEY=generate-a-long-random-secret
